# Server Configuration
PORT=3000

# PostgreSQL Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=metering_billing
DB_USER=postgres
DB_PASSWORD=postgres

# Database URL Connection String
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/metering_billing

# Payment Provider Configuration (fake mode for zero-cost local development)
PAYMENT_PROVIDER=fake
