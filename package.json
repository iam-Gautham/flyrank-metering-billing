{
  "name": "flyrank-metering-billing",
  "version": "1.0.0",
  "description": "FlyRank Usage Metering & Billing Engine",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "db:migrate": "docker exec -i metering_billing_postgres psql -U postgres -d metering_billing < src/db/schema.sql",
    "db:seed": "docker exec -i metering_billing_postgres psql -U postgres -d metering_billing < src/db/seed.sql",
    "test": "node --test tests/**/*.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  }
}
